# Lox

An interpreter for the lox language written in Scala.

## Installation

```sh
brew install sbt
```

## Usage

Running the interactive prompt:

```
sbt run
```

Run a file:

```
sbt "run <file>"
```
